import { BuilderProps } from "../../components/editor/layout";
declare const HomePage: ({ onSubmit, questionData, isReadOnly, config, title, loading, goBackUrl }: BuilderProps) => import("react/jsx-runtime").JSX.Element;
export default HomePage;
